package main

import (
	"fmt"
)

func main() {
	a := []string{"a", "b", "c"}
	b := []string{"b", "c", "d"}

	x, y := SecondQuiz(a, b)
	fmt.Println("Result", x, y)
}

func SecondQuiz(a []string, b []string) ([]string, []string) {

	for i := 0; i < len(a); i++ {

	}

	return a, b
}
